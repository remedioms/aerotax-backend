flask==3.0.3
flask-cors==4.0.1
stripe==10.5.0
anthropic>=0.40.0
pdfplumber==0.11.0
reportlab==4.2.2
gunicorn==22.0.0

Pillow==10.3.0
pillow-heif>=0.16.0
supabase>=2.7.0
google-cloud-tasks>=2.16.0
google-auth>=2.30.0
pytest>=8.0.0
cryptography>=43.0.0
requests>=2.32.0
beautifulsoup4>=4.12.0
feedparser>=6.0
# W-Article: readability-lxml für robuste Main-Content-Extraktion aus
# News-Article-HTML (aero.de, Reuters, AvHerald, SimpleFlying,
# TheAirCurrent, Flightradar24). Liefert deutlich konsistentere
# fulltext-Erkennung als reine Regex-Heuristik; bs4-Heuristik bleibt
# als Fallback wenn das Lib nicht installiert ist.
readability-lxml>=0.8.1
lxml>=5.0
# httpx[http2] benötigt für APNs HTTP/2 (P5/W17). h2-Extra bringt das HTTP/2-Modul.
httpx[http2]>=0.27.0
# W-Ops: Sentry SDK for backend error monitoring (observability/sentry_setup.py)
sentry-sdk[flask]>=2.0.0
# Cloudflare R2 (S3-kompatibel) für Avatar-/Foto-Storage — Zero-Egress, durabel.
boto3>=1.34.0
# pyhafas: GRATIS DB-HAFAS-Client (kein API-Key) für ÖPNV-Routing Haus→Flughafen
# (/api/ax/transit). Nahverkehr-only, ganz DE + Verkehrsverbünde. Defensiv in
# try/except gekapselt — fehlt das Lib oder failt HAFAS, fällt die App auf
# Apple-Transit-ETA zurück.
pyhafas>=0.4.0
