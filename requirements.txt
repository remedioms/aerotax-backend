flask==3.0.3
flask-cors==4.0.1
stripe==10.5.0
anthropic>=0.40.0
pdfplumber==0.11.0
reportlab==4.2.2
gunicorn==22.0.0

Pillow==10.3.0
pillow-heif>=0.16.0
supabase>=2.7.0
google-cloud-tasks>=2.16.0
google-auth>=2.30.0
pytest>=8.0.0
cryptography>=43.0.0
requests>=2.32.0
beautifulsoup4>=4.12.0
feedparser>=6.0
# W-Article: readability-lxml für robuste Main-Content-Extraktion aus
# News-Article-HTML (aero.de, Reuters, AvHerald, SimpleFlying,
# TheAirCurrent, Flightradar24). Liefert deutlich konsistentere
# fulltext-Erkennung als reine Regex-Heuristik; bs4-Heuristik bleibt
# als Fallback wenn das Lib nicht installiert ist.
readability-lxml>=0.8.1
lxml>=5.0
# httpx[http2] benötigt für APNs HTTP/2 (P5/W17). h2-Extra bringt das HTTP/2-Modul.
httpx[http2]>=0.27.0
# W-Ops: Sentry SDK for backend error monitoring (observability/sentry_setup.py)
sentry-sdk[flask]>=2.0.0
# Cloudflare R2 (S3-kompatibel) für Avatar-/Foto-Storage — Zero-Egress, durabel.
boto3>=1.34.0
# ÖPNV-Routing (/api/ax/transit) nutzt gratis kein-Key-Router über reine HTTP-Calls
# (`requests`): MVV-EFA (München) → Transitous/MOTIS → db-rest. KEINE Routing-
# Dependency nötig. tzdata stellt sicher, dass zoneinfo('Europe/Berlin') auf jedem
# (auch slim) Cloud-Run-Image verfügbar ist (EFA braucht lokale Abfahrtszeit).
tzdata>=2024.1
# LH-MQTT-Daemon (lh_mqtt_daemon.py, eigener Compose-Service): Verbindung zum
# Lufthansa-Akamai-Notification-Broker (lhgopenapi.lufthansa.com:8883, TLS,
# JWT-Auth). Nur der Daemon nutzt es — Backend-Worker importieren es nicht.
paho-mqtt>=2.0,<3
# FR24 gRPC per-flight Resolver (blueprints/fr24_grpc.py): anonyme Route/Detail/
# Satelliten-Positionen über data-feed.flightradar24.com (AWS-ELB, NICHT der
# Cloudflare-gewallte feed.js-Pfad — läuft aus Cloud-Run-DC-IP, verifiziert
# 2026-07-07). Zieht httpx(schon da)+protobuf/orjson/isqx/platformdirs (leicht).
fr24>=0.3.1,<0.4
